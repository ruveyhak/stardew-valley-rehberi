import 'package:flutter/material.dart';

class NasilPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Nasıl Oynanır?'),
      ),
      body: Center(
        child: Text('Nasıl Oynanır Sayfası'),
      ),
    );
  }
}