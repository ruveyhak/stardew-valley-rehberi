import 'package:flutter/material.dart';

class EsyaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Eşyalar'),
      ),
      body: Center(
        child: Text('Eşyalar Sayfası'),
      ),
    );
  }
}