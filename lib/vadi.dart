import 'package:flutter/material.dart';

class VadiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Vadi'),
      ),
      body: Center(
        child: Text('Vadi Sayfası'),
      ),
    );
  }
}
