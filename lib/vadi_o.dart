import 'package:flutter/material.dart';

class VadiOPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Vadi Ötesi'),
      ),
      body: Center(
        child: Text('Vadi Ötesi Sayfası'),
      ),
    );
  }
}
