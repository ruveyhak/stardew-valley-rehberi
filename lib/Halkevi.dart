import 'package:flutter/material.dart';

class HalkeviPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Halkevi'),
      ),
      body: Center(
        child: Text('Halkevi Sayfası'),
      ),
    );
  }
}
